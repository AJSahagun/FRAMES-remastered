name: Production

on:
  workflow_run:
    workflows: ["Backend_CI", "Frontend_CI"]
    types:
      - completed
  pull_request:
    branches:
      - main  # This is the 'Production' branch you're targeting for PRs

jobs:
  liquibase:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: "Generate changelog"
        uses: liquibase-github-actions/generate-changelog@v4.30.0
        with:
          changelogFile: "db/changelog/db.changelog-master.sql"
          url: "jdbc:postgresql://ep-misty-brook-a1ejqloe.ap-southeast-1.aws.neon.tech:5432/frames"
          username: "frames_dev"
          password: "nc8qQXku0PJU"
      - name: "Migrate to prod"
        uses: liquibase-github-actions/update@v4.30.0
        with:
          changelogFile: "db/changelog/db.changelog-master.sql"
          url: "jdbc:postgresql://ep-misty-brook-a1ejqloe.ap-southeast-1.aws.neon.tech:5432/frames_prod"
          username: "admin"
          password: "SmsrLv9tdY3a"